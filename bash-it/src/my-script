#!/usr/bin/env bash
set -eou pipefail

dir='/tmp/bin'
src_dir='/var/task'

setup(){
  cp "${src_dir}/cpu-info" "${dir}/"
  #cp "${src_dir}/aws"      "${dir}/"

  source "${dir}/cpu-info"
}

setup
fn

#"${src_dir}/aws" s3 ls
